spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dsbdc
    username: postgres
    password: KaruhurA1707
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
